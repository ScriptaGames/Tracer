{
  "name": "tracer-game",
  "version": "1.0.0",
  "description": "A zeroconf boilerplate for three.js.  No batteries included.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "browser-sync start --server --serveStatic src --files 'src/**/*' --no-inject-changes --directory",
    "css": "postcss --use autoprefixer -o src/css/styles.min.css src/css/styles.css"
  },
  "author": "mwcz <mwc@clayto.com>",
  "license": "GPL-3.0",
  "devDependencies": {
    "autoprefixer": "^6.5.3",
    "browser-sync": "^2.18.2",
    "postcss-cli": "^2.6.0"
  }
}
